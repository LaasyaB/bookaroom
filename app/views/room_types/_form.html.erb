<%= simple_form_for(@room_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :room %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
